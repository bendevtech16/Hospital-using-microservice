spring:
  cloud:
    gateway:
      routes:
       - id: r1
         uri: http://localhost:8081
         predicates:
         - Path: /service-patient/**


  application:
    name: gateway-service

server:
  port: 8888